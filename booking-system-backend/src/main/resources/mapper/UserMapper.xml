<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.booking.mapper.UserMapper">
    <!-- 用户相关SQL映射 -->
    <resultMap id="UserResultMap" type="com.booking.entity.User">
        <id property="userId" column="user_id" />
        <result property="username" column="username" />
        <result property="password" column="password" />
        <result property="email" column="email" />
        <result property="phone" column="phone" />
        <result property="userType" column="user_type" />
        <result property="balance" column="balance" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
        <result property="isDeleted" column="is_deleted" />
    </resultMap>

    <insert id="insert" parameterType="com.booking.entity.User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users(username, password, email, phone, user_type, balance, is_deleted)
        VALUES(#{username}, #{password}, #{email}, #{phone}, #{userType}, #{balance}, #{isDeleted})
    </insert>

    <update id="updateById" parameterType="com.booking.entity.User">
        UPDATE users
        SET username = #{username},
            password = #{password},
            email = #{email},
            phone = #{phone},
            user_type = #{userType},
            balance = #{balance},
            is_deleted = #{isDeleted},
            update_time = NOW()
        WHERE user_id = #{userId}
    </update>

    <select id="selectById" parameterType="int" resultMap="UserResultMap">
        SELECT * FROM users WHERE user_id = #{userId} AND is_deleted = 0
    </select>

    <select id="selectByUsername" parameterType="string" resultMap="UserResultMap">
        SELECT * FROM users WHERE username = #{username} AND is_deleted = 0
    </select>

    <select id="selectByEmail" parameterType="string" resultMap="UserResultMap">
        SELECT * FROM users WHERE email = #{email} AND is_deleted = 0
    </select>

    <select id="selectByPhone" parameterType="string" resultMap="UserResultMap">
        SELECT * FROM users WHERE phone = #{phone} AND is_deleted = 0
    </select>

    <select id="selectAll" resultMap="UserResultMap">
        SELECT * FROM users WHERE is_deleted = 0
    </select>

    <update id="deleteById" parameterType="int">
        UPDATE users SET is_deleted = 1 WHERE user_id = #{userId}
    </update>
</mapper>
